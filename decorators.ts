// ----------------------------------------------------------------------------
// Decorators (an experimental feature)
// ----------------------------------------------------------------------------

// you can modify a behavior of class, method or property at runtime
// normally you won't need it

// must be enabled explicitly in tsconfig.json if we want to write our own
//experimentalDecorators
//emitDecoratorMetadata

// these are basically annotations

// but it's used internally by ts, so it's good to know them

// @Component({})
// class Component {
//     constructor(name: string) {

//     }
// }

